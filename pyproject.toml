[project]
name = "agentic-ai-em-to-fde-journey"
version = "0.1.0"
description = "getting started with llm and python hello world"
readme = "README.md"
authors = [
    { email = "ziad.sv@outlook.com" }
]
requires-python = ">=3.9"
dependencies = [
    "openai>=2.48.0",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.1",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
